# ── Core ─────────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.0.0
rich>=13.0.0          # pretty terminal output

# ── AI / LLM ─────────────────────────────────────────────────────────────────
anthropic>=0.25.0     # Claude API (Anthropic)
openai>=1.0.0         # optional OpenAI fallback
langchain>=0.1.0
langchain-anthropic>=0.1.0

# ── ML / GNN ─────────────────────────────────────────────────────────────────
# NOTE for ARM64: install torch BEFORE torch-geometric
# Run: pip install torch --index-url https://download.pytorch.org/whl/cpu
# Then: pip install torch-geometric
torch>=2.1.0                      # ARM64 wheels available via pytorch.org
scikit-learn>=1.3.0
numpy>=1.24.0
sentence-transformers>=2.2.0      # ARM64-compatible

# ── Memory / Storage ─────────────────────────────────────────────────────────
# ChromaDB is optional. PsychologyMemory automatically uses SQLite when it
# is unavailable, which is the supported default for Windows ARM64.
# Install requirements-chroma.txt separately to enable semantic search.

# ── Data / Graphs ─────────────────────────────────────────────────────────────
networkx>=3.1
faker>=19.0.0         # synthetic log / user generation
pandas>=2.0.0

# ── Security / Standards ─────────────────────────────────────────────────────
stix2>=3.0.0          # STIX 2.1 bundle generation
mitreattack-python>=2.0.0  # MITRE ATT&CK lookups

# ── Visualization ─────────────────────────────────────────────────────────────
streamlit>=1.28.0
plotly>=5.17.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.4.0
